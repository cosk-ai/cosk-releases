coskey 0.1.0 — 首个对外发布版本

- ChatGPT.app 零修改、不重打包，灵活切换第三方大模型：会话
  进行中也能换（下一轮即生效）；上下文窗口大小按模型自行指定
- 只有 /chat/completions 的模型服务也能用：本地自动做 responses ⇄
  chat/completions 双向翻译，流式输出与工具调用照旧
- 解决「会话 provider 失效、打开会话就报错」：自动归因、接管修复，再次打开时
  自愈；不想再管的会话可一键转交回官方
