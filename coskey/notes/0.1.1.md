Coskey 0.1.1 — 应用名与菜单栏改进

- 应用名统一为 Coskey：菜单栏、Dock 与 Finder 里都显示为大写开头的
  Coskey（此前是小写 coskey）。已装的版本更新后就地生效，不用重装
- 新增「关于 Coskey」菜单：一处看到当前版本、更新镜像与上次检查时间，
  并直达用户协议与隐私政策
- 新增「检查更新」菜单：不必进设置页也能随时手动查一次；已是最新会
  明确回一句，检查失败也会直接报错，不再让人猜
